# AI provider keys belong only in local or deployment secrets.
# Never commit real credentials.
# For local Next.js development, copy these values to apps/web/.env.local.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite
NEXT_PUBLIC_SITE_URL=http://localhost:3000
